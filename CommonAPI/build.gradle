plugins {
    id 'org.spongepowered.gradle.vanilla' version '0.2.1-SNAPSHOT'
}

group = "${mod_group}.api"

base {
    archivesName = "${mod_id}-${mc_version}-common-api"
}

minecraft {
    version("${mc_version}")
}

