import net.darkhax.curseforgegradle.TaskPublishCurseForge
import net.darkhax.curseforgegradle.Constants as CFG_Constants

plugins {
    id 'com.modrinth.minotaur' version '2.+'
    id 'net.neoforged.gradle.userdev' version '7.0.96'
}

runs {

    client {
        systemProperty 'forge.logging.markers', 'REGISTRIES'
        systemProperty 'forge.logging.console.level', 'debug'
        systemProperty 'forge.enabledGameTestNamespaces', "${project.mod_id}"

        modSource project.sourceSets.main

        dependencies {
            runtime project(":CommonAPI")
        }
    }

}

dependencies {
    implementation "net.neoforged:neoforge:${neo_version}"

    compileOnly project(":CommonAPI")
}

subsystems {
    parchment {
        minecraftVersion = "1.20.3"
        mappingsVersion = "${parchmentVersion}"
    }
}

tasks.register("publishToCurseforgeNeoforgeVersion", TaskPublishCurseForge) {
    dependsOn(tasks.jar)

    apiToken = "${curseforgeToken}"

    var mainFile = upload(curseforgeProjectId, tasks.jar.getArchiveFile())
    mainFile.changelog = "Change cfg system"
    mainFile.changelogType = CFG_Constants.CHANGELOG_TEXT
    mainFile.releaseType = CFG_Constants.RELEASE_TYPE_RELEASE
    mainFile.addGameVersion("${mc_version}")
    mainFile.addModLoader("NeoForge")

}

modrinth {
    token = "mrp_r2LoPKuUaEeViOAeDLNB0oOh6WaOetj7B2nbKjkCxNLIn9QyKXFCKIEMchuw"
    projectId = "STb5IL4E"
    versionName = "GuidebookAPI-Neoforge"
    uploadFile = jar
    gameVersions = ["1.20.4"]
    loaders = ["neoforge"]
}